#include <stdio.h>
#include <stdbool.h>
#include "txtfind.h"
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
int ARR[LENGTH];
int ARR2[LENGTH2];
int main()
{
    int get_line=0;
    char alltext[LINE];
    get_line = print_line(alltext);
}