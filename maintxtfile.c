#include <stdio.h>
#include <stdbool.h>
#include "array.h"
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
int ARR[length];
int ARR2[length2];
int main()
{
    int get_line=0;
    char alltext[LINE];
    get_line = print_line(alltext);
}